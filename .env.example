# ── Secrets (REQUIRED for any real deployment — generate fresh) ───────────────
# Encrypts stored integration/SSO secrets. A 32-byte urlsafe-base64 key:
#   python3 -c "import base64,os;print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
# If you leave this empty, a public dev key is used (fine for local eval ONLY).
ELLIPTIC_KEK=
# A long random string. Generate:  openssl rand -hex 32
JWT_SECRET_KEY=

# ── Mode ──────────────────────────────────────────────────────────────────────
# development = http-friendly (non-secure cookies) for local evaluation.
# production  = secure cookies (serve over HTTPS) AND enforces strong secrets above.
ENV=development

# ── Origins (the browser-facing web URL) ──────────────────────────────────────
CORS_ORIGINS=http://localhost:3000
APP_BASE_URL=http://localhost:3000

# ── Postgres ──────────────────────────────────────────────────────────────────
POSTGRES_USER=companyos
POSTGRES_PASSWORD=companyos
POSTGRES_DB=companyos

# ── Host port overrides (optional) ────────────────────────────────────────────
# API_PORT=8000
# WEB_PORT=3000

# ── MCP / OAuth (agent gateway) ───────────────────────────────────────────────
# OAUTH_ISSUER: public origin of this API (issuer in OAuth discovery).
# MCP_RESOURCE_BASE: canonical MCP resource URI (RFC 8707/9728) — MUST be the
#   full URL clients connect to, INCLUDING the /api/v1/mcp path, or the OAuth
#   flow 400s with "resource must equal the canonical MCP URI".
# In production these are set on the api service (see docker-compose.override.yml
# on the host), e.g.:
#   OAUTH_ISSUER=https://api.elliptic.sh
#   MCP_RESOURCE_BASE=https://api.elliptic.sh/api/v1/mcp
# OAUTH_ISSUER=http://localhost:8000
# MCP_RESOURCE_BASE=http://localhost:8000/api/v1/mcp

# ── Live co-editing relay (optional; falls back to plain save when unset) ─────
# NEXT_PUBLIC_REALTIME_URL=

# ── Web → API origin (compose-internal default; baked into the web image at
#    build time — change only if not running web via this compose) ────────────
# BACKEND_ORIGIN=http://api:8000

# ── Object storage (S3-compatible; Cloudflare R2 by default) ─────────────────
# Uploads fail closed when these are unset: the Drive, comment attachments and
# every download return "Object storage is not configured". The whole feature is
# optional — leave them blank and the rest of the product runs normally.
#
# R2_ENDPOINT_URL, R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY are the three that
# switch storage on; R2_BUCKET and R2_ACCOUNT_ID have working defaults.
#
# Any S3-compatible store works. On Cloudflare R2 the access key pair comes from
# an R2 API token (dashboard → R2 → Manage API tokens → Object Read & Write),
# and R2 shows the secret once, at creation, and never again. On MinIO it comes
# from a service account. The names keep the R2_ prefix either way. Note that
# boto3 signs a path style URL for a custom endpoint.
#
# The browser PUTs straight to the bucket, so the store must also allow your web
# origin with GET, PUT and HEAD. R2 takes a per-bucket CORS rule. MinIO takes the
# origins in MINIO_API_CORS_ALLOW_ORIGIN. Without it uploads work over MCP and
# the API but fail in the browser. See DEPLOY.md.
# R2_ENDPOINT_URL=https://<account_id>.r2.cloudflarestorage.com
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_ACCOUNT_ID=
# R2_BUCKET=elliptic-media
