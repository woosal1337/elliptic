# Local environment / secrets
.env
.env.local
*.local
.vercel

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# (Per-app .gitignore files under apps/api and apps/web cover their own build
#  artifacts, node_modules, .venv, .next, .turbo, caches, etc.)

# Python bytecode from scripts/
__pycache__/
*.pyc

# Written by scripts/screenshots/seed_showcase.py. It holds the ids of a
# throwaway local workspace, and they change on every run.
scripts/screenshots/showcase.json
