apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "elliptic.name" . }}-api
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels: { app: {{ include "elliptic.name" . }}-api }
  template:
    metadata:
      labels: { app: {{ include "elliptic.name" . }}-api }
    spec:
      initContainers:
        - name: migrate
          image: {{ .Values.api.image | quote }}
          command: ["alembic", "upgrade", "head"]
          envFrom:
            - secretRef: { name: {{ include "elliptic.name" . }}-secrets }
      containers:
        - name: api
          image: {{ .Values.api.image | quote }}
          ports: [{ containerPort: {{ .Values.api.port }} }]
          envFrom:
            - secretRef: { name: {{ include "elliptic.name" . }}-secrets }
          livenessProbe:
            httpGet: { path: /api/v1/health, port: {{ .Values.api.port }} }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /api/v1/health, port: {{ .Values.api.port }} }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources: {{- toYaml .Values.api.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "elliptic.name" . }}-api
spec:
  selector: { app: {{ include "elliptic.name" . }}-api }
  ports: [{ port: {{ .Values.api.port }}, targetPort: {{ .Values.api.port }} }]
