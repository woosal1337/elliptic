{
  "name": "elliptic-web",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint && node scripts/check-theme-tokens.mjs",
    "typecheck": "turbo run typecheck",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "turbo": "^2.9.18"
  },
  "dependencies": {
    "@tiptap/extension-list": "^3.26.1"
  },
  "version": "1.6.0",
  "license": "Apache-2.0"
}
