/**
 * Corner-radius scale (ported from the web design tokens).
 * Buttons/inputs use `md`, cards `lg`, chips/badges/avatars `full`.
 */
export const radius = {
  xs: 4,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
  xxl: 24,
  full: 999,
} as const
