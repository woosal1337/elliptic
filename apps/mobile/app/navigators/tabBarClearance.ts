/**
 * Bottom padding a scrolling list needs so its last row clears the native tab
 * bar (~49pt) and the home indicator, letting content scroll under the glass
 * instead of being cut off by it.
 */
export const TAB_BAR_CLEARANCE = 96
